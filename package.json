{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir -p dist && tailwindcss -i style.css -o dist/style.css --minify && cp index.html dist/index.html && cp -r writing dist/writing && cp -r public dist/public",
    "deploy": "npx wrangler pages deploy dist --project-name=nbaglivo-dev"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@tailwindcss/vite": "^4.3.3",
    "html-validate": "^11.8.0",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.1"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
