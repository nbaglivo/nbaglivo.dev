// Intentionally empty: satisfies Vite's requirement for at least one build
// entrypoint so plugin hooks can run. The compiled chunk is deleted from
// dist/ by the site plugin before the build finishes.
